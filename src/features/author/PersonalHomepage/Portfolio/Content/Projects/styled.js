import styled from "styled-components";

export const List = styled.ul`
    margin: 24px;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    list-style: none;
    padding: 0;
    grid-gap: 32px;
`;